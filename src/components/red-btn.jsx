const RedBtn = ({ btnText }) => {
  return (
    <button className="bg-bgRedColor drop-shadow-red text-white font-medium text-sm p-2 rounded-xl h-[45px]">
      {btnText}
    </button>
  );
};

export default RedBtn;
